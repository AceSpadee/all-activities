// WRITE YOUR CODE HERE

const names = ["peyton", "bob", "steve"];

console.log(names);

console.log(`welcome to the class, ${names}.`);

console.log(`the are ${names.length} students`);

if (names[0] = "spencer") {
    console.log(`${names[0]} is in the class`)
};

console.log(`the new list of names are ${names}`);
// -_-----------------------------------------------------------------------------------

const students = ["jason", "david", "diego", "frank", "alexandria"];

console.log(students.length)
/*
console.log(`welcome to the class, ${names[0]}`);
console.log(`welcome to the class, ${names[1]}`);
console.log(`welcome to the class, ${names[2]}`);
console.log(`welcome to the class, ${names[3]}`);
console.log(`welcome to the class, ${names[4]}`);
*/
students[0] = "elena";

if (students[0] = "elena") {
    console.log(`${names[0]} is in the class`)
};

for (people of students) {
    console.log(`welcome to the class ${people}`)
}