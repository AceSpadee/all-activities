// TODO: What will the following line of code log?

                // access the windows. logs the windows object
                // logs the window object. it contains all the other objects in the browser
console.log(window);

// TODO: What will the following line of code log?

                // it returns the document location
                // it contains everything in the document 
console.log(window.document);

// TODO: What will the following line of code log?

                // accesses all the elements in the html
                // the document elements
console.log(document.documentElement);

// TODO: What will the following line of code log?

                // access logs the html head element
                // list the html dom nodes within the head element
console.log(document.head);

